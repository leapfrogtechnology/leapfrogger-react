const BASE_URI = 'https://lms.lftechnology.com';
const api = '/api';

export const uri = {
  EMAIL_VALIDATION: `${BASE_URI}${api}/emailvalidation`,
  EMPLOYEES_LIST: `${BASE_URI}${api}/employeelist`,
  DEPARTMENT_LIST: `${BASE_URI}${api}/departmentlist`, 
};
